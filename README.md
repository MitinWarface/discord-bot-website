# Aurora Discord Bot

Aurora - многофункциональный Discord бот с широким спектром возможностей для модерации, развлечений и взаимодействия с сообществом.

## ✨ Особенности

### 🛠 Модерация
- Автоматическая модерация (фильтр мата, спама, ссылок)
- Команда временного мута пользователя (`/mute`)
- Команда очистки сообщений с различными опциями (`/clear`)
- Система предупреждений (`/warn`)
- Система тикетов с кнопками (`/ticket`)
- Логирование событий сервера

### 📊 Система уровней и прогресса
- Система уровней и XP за активность
- Система квестов с наградами
- Система гильдий
- Система репутации (`/rep`)
- Команда просмотра профиля (`/profile`)
- Таблица лидеров (`/leaderboard`)

### 🎉 Развлечения
- Ролеплей-команды с гифками (`/hug`, `/pat`, `/kill`)
- Генераторы контента (мемы, животные, шутки) (`/meme`, `/cat`, `/dog`, `/fox`, `/joke`)
- Система мини-игр и викторин (`/trivia`)
- Музыкальные команды с очередью (`/play`, `/skip`, `/stop`, `/queue`)

### 💎 Экономика
- Внутрисерверная валюта и система баланса (`/economy`)
- Ежедневная награда (`/economy daily`)
- Работа с наградой (`/economy work`)
- Магазин с покупками (`/economy shop`)
- Переводы между пользователями (`/economy transfer`)
- Таблица лидеров по монетам (`/economy leaderboard`)

### 🗳️ Дополнительные функции
- Команда погоды (`/weather`)
- Система голосований с кнопками (`/poll`)
- Система ролей по реакции (`/reactionrole`)
- Система кастомных команд
- Веб-панель управления с аутентификацией через Discord

## 🚀 Установка

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/username/aurora-bot.git
   cd aurora-bot
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env` и укажите следующие переменные:
   ```
   # Токен Discord бота
   DISCORD_TOKEN=your_discord_bot_token
   
   # ID сервера (опционально)
   GUILD_ID=your_guild_id
   
   # Токен YouTube Data API v3
   YOUTUBE_TOKEN=your_youtube_api_key
   
   # Настройки Lavalink
   LAVALINK_HOST=lavalink_host
   LAVALINK_PORT=lavalink_port
   LAVALINK_PASSWORD=lavalink_password
   LAVALINK_SECURE=false
   LAVALINK_RESUME_KEY=aurora-music
   
   # Ключ OpenWeatherMap API
   OPENWEATHER_API_KEY=your_openweather_api_key
   
   # Настройки веб-панели
   SESSION_SECRET=your_session_secret
   BASE_URL=http://localhost:3000
   DISCORD_CLIENT_ID=your_discord_client_id
   DISCORD_CLIENT_SECRET=your_discord_client_secret
   
   # Настройки системы уровней
   XP_PER_MESSAGE_MIN=5
   XP_PER_MESSAGE_MAX=15
   XP_COOLDOWN_MS=60000
   LEVEL_UP_BONUS=10
   
   # Настройки экономической системы
   COINS_PER_DAILY=100
   COINS_PER_WORK_MIN=50
   COINS_PER_WORK_MAX=150
   WORK_COOLDOWN_HOURS=12
   DAILY_STREAK_BONUS=10
   MAX_BANK_CAPACITY=50000
   
   # Настройки автомодерации
   AUTOMOD_ENABLED=true
   PROFANITY_FILTER=true
   LINKS_FILTER=false
   SPAM_FILTER=true
   CAPS_FILTER=false
   INVITES_FILTER=true
   WARN_THRESHOLD=3
   MUTE_THRESHOLD=5
   KICK_THRESHOLD=8
   BAN_THRESHOLD=10
   
   # Настройки логирования
   LOGGING_ENABLED=true
   MODERATION_LOG_CHANNEL=moderation_log_channel_id
   MEMBER_LOG_CHANNEL=member_log_channel_id
   MESSAGE_LOG_CHANNEL=message_log_channel_id
   ```

4. Запустите бота:
   ```bash
   node index.js
   ```

## 📝 Команды

### Модерация
- `/warn <user> [reason]` - Выдать предупреждение пользователю
- `/mute <user> <duration> [reason]` - Замутить пользователя на определенное время
- `/kick <user> [reason]` - Выгнать пользователя с сервера
- `/ban <user> [reason]` - Заблокировать пользователя на сервере
- `/clear <amount> [user]` - Очистить сообщения
- `/ticket create <subject>` - Создать тикет

### Уровни и прогресс
- `/profile` - Просмотр профиля пользователя
- `/leaderboard` - Таблица лидеров по очкам
- `/level` - Проверить уровень и XP
- `/quests` - Система квестов
- `/guild` - Система гильдий
- `/rep <user>` - Выдать репутацию пользователю

### Развлечения
- `/hug <user>` - Обнять пользователя
- `/pat <user>` - Погладить пользователя
- `/kill <user>` - "Убить" пользователя (в игровой форме)
- `/meme` - Случайный мем
- `/cat` - Случайное изображение кота
- `/dog` - Случайное изображение собаки
- `/fox` - Случайное изображение лисы
- `/joke` - Случайная шутка
- `/trivia` - Викторина

### Экономика
- `/economy balance [user]` - Проверить баланс
- `/economy daily` - Получить ежедневную награду
- `/economy work` - Работать и получить награду
- `/economy transfer <user> <amount>` - Перевести монеты
- `/economy leaderboard` - Таблица лидеров по монетам
- `/economy shop` - Открыть магазин

### Дополнительные функции
- `/weather <city>` - Погода в указанном городе
- `/poll <question> <option1> <option2> [option3] [option4] [option5] [duration]` - Создать голосование
- `/reactionrole setup` - Настроить сообщение с реакциями для ролей
- `/automod settings` - Настройка автоматической модерации

## 🌐 Веб-панель управления

Бот включает веб-панель управления с аутентификацией через Discord. Панель доступна по адресу, указанному в переменной `BASE_URL`.

## 🤝 Вклад

Если вы хотите внести вклад в развитие бота, пожалуйста, создайте issue или pull request.

## 📄 Лицензия

MIT License
