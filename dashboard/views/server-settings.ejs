<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Настройки сервера | Aurora Bot Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 10%);
            min-height: 100vh;
            color: white;
        }

        .header {
            background: rgba(0, 0, 0, 0.2);
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: linear-gradient(135deg, #8b00ff 0%, #4a00e0 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        .nav {
            display: flex;
            gap: 20px;
        }

        .nav a {
            color: white;
            text-decoration: none;
            padding: 10px 15px;
            border-radius: 8px;
            transition: background 0.3s ease;
        }

        .nav a:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }

        .server-header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 30px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 25px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .server-icon {
            width: 80px;
            height: 80px;
            border-radius: 15px;
            background: linear-gradient(135deg, #8b00ff 0%, #4a00e0 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
        }

        .server-info h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }

        .server-info p {
            opacity: 0.7;
        }

        .settings-container {
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 30px;
        }

        .sidebar {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 20px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .sidebar h3 {
            margin-bottom: 20px;
            font-size: 18px;
        }

        .sidebar-menu {
            list-style: none;
        }

        .sidebar-menu li {
            margin-bottom: 10px;
        }

        .sidebar-menu a {
            display: block;
            padding: 12px 15px;
            border-radius: 8px;
            text-decoration: none;
            color: white;
            transition: background 0.3s ease;
        }

        .sidebar-menu a:hover, .sidebar-menu a.active {
            background: rgba(255, 255, 255, 0.2);
        }

        .settings-content {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .settings-content h2 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
        }

        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border-radius: 8px;
            border: none;
            background: rgba(0, 0, 0, 0.2);
            color: white;
            font-size: 16px;
        }

        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
        }

        .checkbox-group input[type="checkbox"] {
            width: auto;
        }

        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s ease;
            background: #7289DA;
            color: white;
            font-size: 16px;
        }

        .btn:hover {
            background: #677bc4;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="user-info">
            <div class="avatar">👤</div>
            <div>
                <h3><%= user.username %></h3>
                <p>ID: <%= user.id %></p>
            </div>
        <div class="nav">
            <a href="/servers">Серверы</a>
            <a href="/auth/logout">Выход</a>
        </div>
    </div>

    <div class="container">
        <div class="server-header">
            <div class="server-icon">🏰</div>
            <div class="server-info">
                <h1><%= server.name %></h1>
                <p>ID сервера: <%= server.id %></p>
            </div>
        </div>

        <div class="settings-container">
            <div class="sidebar">
                <h3>Настройки</h3>
                <ul class="sidebar-menu">
                    <li><a href="#" class="active">Общие</a></li>
                    <li><a href="#">Модерация</a></li>
                    <li><a href="#">Уровни</a></li>
                    <li><a href="#">Экономика</a></li>
                    <li><a href="#">Тикеты</a></li>
                    <li><a href="#">События</a></li>
                </ul>
            </div>

            <div class="settings-content">
                <h2>Общие настройки</h2>
                <form id="settingsForm">
                    <div class="form-group">
                        <label for="prefix">Префикс команд</label>
                        <input type="text" id="prefix" name="prefix" value="<%= settings.prefix %>">
                    </div>
            
                    <div class="form-group">
                        <h3>Модерация</h3>
                        <div class="checkbox-group">
                            <input type="checkbox" id="automod-enabled" name="automod[enabled]" <%= settings.automod.enabled ? 'checked' : '' %>>
                            <label for="automod-enabled">Включить автомодерацию</label>
                        </div>
                        
                        <div class="checkbox-group">
                            <input type="checkbox" id="automod-profanity" name="automod[filter][profanity]" <%= settings.automod.filter.profanity ? 'checked' : '' %>>
                            <label for="automod-profanity">Фильтр ненормативной лексики</label>
                        </div>
                        
                        <div class="checkbox-group">
                            <input type="checkbox" id="automod-links" name="automod[filter][links]" <%= settings.automod.filter.links ? 'checked' : '' %>>
                            <label for="automod-links">Фильтр ссылок</label>
                        </div>
                        
                        <div class="checkbox-group">
                            <input type="checkbox" id="automod-spam" name="automod[filter][spam]" <%= settings.automod.filter.spam ? 'checked' : '' %>>
                            <label for="automod-spam">Фильтр спама</label>
                        </div>
                        
                        <div class="checkbox-group">
                            <input type="checkbox" id="automod-caps" name="automod[filter][caps]" <%= settings.automod.filter.caps ? 'checked' : '' %>>
                            <label for="automod-caps">Фильтр капса</label>
                        </div>
                        
                        <div class="checkbox-group">
                            <input type="checkbox" id="automod-invites" name="automod[filter][invites]" <%= settings.automod.filter.invites ? 'checked' : '' %>>
                            <label for="automod-invites">Фильтр приглашений на другие серверы</label>
                        </div>
                    </div>
            
                    <div class="form-group">
                        <h3>Логирование</h3>
                        <div class="checkbox-group">
                            <input type="checkbox" id="logging-enabled" name="logging[enabled]" <%= settings.logging.enabled ? 'checked' : '' %>>
                            <label for="logging-enabled">Включить логирование</label>
                        </div>
                        
                        <label for="log-channel">ID канала для логов</label>
                        <input type="text" id="log-channel" name="logging[logChannel]" value="<%= settings.logging.logChannel || '' %>">
                        <p style="font-size: 12px; opacity: 0.7; margin-top: 5px;">Введите ID текстового канала для отправки логов</p>
                    </div>
            
                    <div class="form-group">
                        <h3>Система уровней</h3>
                        <div class="checkbox-group">
                            <input type="checkbox" id="leveling-enabled" name="leveling[enabled]" <%= settings.leveling.enabled ? 'checked' : '' %>>
                            <label for="leveling-enabled">Включить систему уровней</label>
                        </div>
                        
                        <label for="xp-min">Минимальное количество XP за сообщение</label>
                        <input type="number" id="xp-min" name="leveling[xpPerMessageMin]" value="<%= settings.leveling.xpPerMessageMin %>" min="1" max="100">
                        
                        <label for="xp-max">Максимальное количество XP за сообщение</label>
                        <input type="number" id="xp-max" name="leveling[xpPerMessageMax]" value="<%= settings.leveling.xpPerMessageMax %>" min="1" max="100">
                    </div>
            
                    <div class="form-group">
                        <h3>Экономика</h3>
                        <div class="checkbox-group">
                            <input type="checkbox" id="economy-enabled" name="economy[enabled]" <%= settings.economy.enabled ? 'checked' : '' %>>
                            <label for="economy-enabled">Включить экономическую систему</label>
                        </div>
                        
                        <label for="daily-amount">Количество монет за ежедневную награду</label>
                        <input type="number" id="daily-amount" name="economy[dailyAmount]" value="<%= settings.economy.dailyAmount %>" min="1" max="1000">
                    </div>
            
                    <div class="form-group">
                        <h3>Система тикетов</h3>
                        <div class="checkbox-group">
                            <input type="checkbox" id="tickets-enabled" name="tickets[enabled]" <%= settings.tickets.enabled ? 'checked' : '' %>>
                            <label for="tickets-enabled">Включить систему тикетов</label>
                        </div>
                        
                        <label for="tickets-category">ID категории для тикетов</label>
                        <input type="text" id="tickets-category" name="tickets[categoryId]" value="<%= settings.tickets.categoryId || '' %>">
                        <p style="font-size: 12px; opacity: 0.7; margin-top: 5px;">Введите ID категории, в которой будут создаваться тикеты</p>
                    </div>
            
                    <button type="submit" class="btn">Сохранить настройки</button>
                </form>
            </div>
        </div>
    </div>

    <script>
        document.getElementById('settingsForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const formData = new FormData(this);
            const settings = {};
            
            // Сбор данных формы в объект
            for (let [key, value] of formData.entries()) {
                // Обработка вложенных свойств
                if (key.includes('[')) {
                    const keys = key.replace(/]/g, '').split('[');
                    let current = settings;
                    
                    for (let i = 0; i < keys.length - 1; i++) {
                        if (!current[keys[i]]) {
                            current[keys[i]] = {};
                        }
                        current = current[keys[i]];
                    }
                    
                    current[keys[keys.length - 1]] = value === 'on' ? true : value;
                } else {
                    settings[key] = value;
                }
            }
            
            // Отправка данных на сервер
            fetch(`/api/server/<%= server.id %>/settings`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(settings)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert('Настройки успешно сохранены!');
                } else {
                    alert('Ошибка при сохранении настроек: ' + data.message);
                }
            })
            .catch(error => {
                console.error('Ошибка:', error);
                alert('Произошла ошибка при сохранении настроек');
            });
        });
    </script>
</body>
</html>